# Configurações de Email SMTP
# Configure estas variáveis com suas credenciais de email

# Host do servidor SMTP (ex: smtp.gmail.com, smtp.office365.com)
SMTP_HOST=smtp.gmail.com

# Porta do servidor SMTP (587 para TLS, 465 para SSL)
SMTP_PORT=587

# Use 'true' para porta 465 (SSL), 'false' para porta 587 (TLS)
SMTP_SECURE=false

# Email de autenticação SMTP
SMTP_USER=seu-email@gmail.com

# Senha ou App Password do email
SMTP_PASSWORD=sua-senha-aqui

# Email remetente (geralmente o mesmo que SMTP_USER)
SMTP_FROM=seu-email@gmail.com

# Email que receberá as mensagens do formulário de contato
CONTACT_EMAIL=seu-email@gmail.com
